---
import type { SectionHeaderProps } from '@/types';

const { title, class: className = "" } = Astro.props as SectionHeaderProps;
---

<h2 class={`text-sm font-medium text-gray-500 mb-6 ${className}`}>{title}</h2>
